apiVersion: v1
kind: Service
metadata:
  name: {{ include "directory-service.fullname" . }}
  labels:
    {{- include "directory-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "directory-service.selectorLabels" . | nindent 4 }}
